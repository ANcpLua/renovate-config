{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/assert-maf-config.mjs",
    "validate:renovate": "npx --yes renovate-config-validator --strict --no-global default.json",
    "validate": "npm run test"
  },
  "devDependencies": {
    "renovate": "43.130.0"
  }
}
